name: skilltrade
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.3
  cloud_firestore: ^5.6.7
  firebase_storage: ^12.4.1
  font_awesome_flutter: ^10.8.0
  image_picker: ^1.1.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3  # <-- Added for icon generation

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/st_icon.jpg"

flutter:
  uses-material-design: true
  assets:
    - assets/default_profile.png
    - assets/icon/st_icon.jpg
